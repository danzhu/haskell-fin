let twice = |f| |a| f (f a)
let add1 = add 1
def id a = a
def fib = |{
    0| 0
    1| 1
    n| (fib (n .sub 1)) .add (fib (n .sub 2))
}
println (twice add1 2)
println {
    let n = 10
    id fib n
}
println "hello world"
